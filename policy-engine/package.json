{
  "name": "@neurcode/policy-engine",
  "version": "0.1.0",
  "description": "Policy and rule evaluation engine for Neurcode",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "policy",
    "rules",
    "governance"
  ],
  "license": "MIT",
  "dependencies": {
    "@neurcode/diff-parser": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}

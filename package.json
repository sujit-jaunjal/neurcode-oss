{
  "name": "neurcode-oss",
  "version": "0.1.0",
  "private": true,
  "description": "Neurcode Open Source Packages",
  "workspaces": [
    "cli",
    "diff-parser",
    "policy-engine",
    "github-action",
    "vscode-ext"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}

